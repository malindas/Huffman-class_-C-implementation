#pragma once
#include <iostream>
#include <map>
#include <vector>
using namespace std;

template<class sym, class prob>
class Huffman {

public:
	map<sym, bool> huffmandict(sym * symbol, prob * probability)

private:



};

template<class sym, class prob>
inline map<sym, bool> huffmandict(sym * symobl, prob * probability) {

}